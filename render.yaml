services:
  - type: web
    name: carenav-ai-healthcare
    env: node
    plan: free
    region: oregon
    buildCommand: npm --prefix healthcare-system install
    startCommand: npm --prefix healthcare-system start
    envVars:
      - key: NODE_VERSION
        value: 22.12.0
      - key: PORT
        value: 10000
